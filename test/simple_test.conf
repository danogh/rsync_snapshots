#!/usr/bin/perl

my @options=();
if( $ENV{EXCLUDE} ){
    for my $e (split(/,/, $ENV{EXCLUDE})){
	push(@options, "exclude=$e");
    }
}

rsync_snapshot($ENV{SRC}, $ENV{DST}, "$ENV{EXPIRE}", @options);
